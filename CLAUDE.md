# Claude Code instructions

Read `AGENTS.md` and `START_HERE.md` before changing anything. The rules in `AGENTS.md` are mandatory. Use `.claude/skills/cro-fit-score` for job scoring and `.claude/skills/cco-whatsapp` for read-only WhatsApp analysis. Neither workflow uses an LLM API key.

